# 验收要点（lab1–lab8 代码行为说明）

## 1. 填空总览

`2ba7975` 删除了 29 行 `TODO(labN)` 注释，即填了 29 个空，分布在 8 个文件：

| lab | 文件 | 空数 | 一句话 |
|---|---|---|---|
| lab1 | `camera_worker.cpp` | 2 | 采集循环：水平镜像 + 发布共享帧 |
| lab1 | `display_viewmodel.cpp` | 7 | 图片读取、摄像头启停、模式切换 |
| lab2 | `capture_source.cpp` | 3 | 源阶段取一帧 |
| lab2 | `sobel_stage.cpp` | 2 | 校验 + Sobel 边缘 |
| lab2 | `pipeline_viewmodel.cpp` | 2 | 装配灰度→Sobel 两个 stage |
| lab5 | `amsler_test.cpp` | 6 | Amsler 状态机 |
| lab6 | `eye_detection.cpp` | 4 | ROI 二值化找瞳孔 |
| lab6 | `measurement_worker.cpp` | 3 | 超时内取帧测量 |

注意：**只有这 8 个文件是"填空"，其余文件从头就是完整的**。`common/` 是老师给的框架，验收时不要说自己改了它。

---

## 2. 逐空细节（lab1 / lab2 / lab5 / lab6）

### 2.1 lab1 图像与视频显示

**`camera_worker.cpp`**（采集线程，`run()` 内）

- 空1「把这一帧水平镜像」→ `cv::flip(frame->mat, frame->mat, 1)`（[camera_worker.cpp:89](lab1/camera_worker.cpp#L89)）。参数 `1` = 绕 y 轴翻转，即左右镜像，符合前置摄像头预览习惯。
- 空2「打包成 Frame 并通过 `m_onFrame` 发布」→ `m_onFrame(std::make_shared<const labcore::Frame>(std::move(*frame)))`（[:90](lab1/camera_worker.cpp#L90)）。`const` 共享 + `move`，符合"帧不可变、零拷贝传递"的设计。
- 循环节拍由 `kFramePeriod = 33ms` 控制，并用 `steady_clock` 扣掉已耗时，所以是**补偿式定时**而不是简单 sleep(33)。

**`display_viewmodel.cpp`**

- 「读取所选图片；失败要报错并返回 false」→ `cv::imread(path.toStdString(), cv::IMREAD_UNCHANGED)`，再经 `labqt::isSupported()` 与 `toQImage()` 双重校验，失败 `emit errorOccurred(...)` + `return false`（[:30-40](lab1/display_viewmodel.cpp#L30-L40)）。用 `IMREAD_UNCHANGED` 是为了保留 alpha/位深。
- 「显示图片前先停止摄像头采集（图片与摄像头互斥）」→ 函数第一行 `stopCapture()`（[:28](lab1/display_viewmodel.cpp#L28)）。
- 「把这一帧交给界面并切换到图片模式」→ `emit frameChanged(converted); setMode(Mode::Photo);`（[:42-43](lab1/display_viewmodel.cpp#L42-L43)）。
- 「启动采集线程，把最新帧写入线程安全槽（失败要报错返回 false）」→ `m_worker.start(callback)`，回调里 `m_latest.set(labqt::toQImage(frame->mat))`（[:51-58](lab1/display_viewmodel.cpp#L51-L58)）。
- 「启动刷新定时器并切换到视频模式」→ `m_frameTimer->start(); setMode(Mode::Video);`（[:64-65](lab1/display_viewmodel.cpp#L64-L65)）。
- 「停止采集线程与刷新定时器」→ `stopCapture()`：`m_worker.stop()` + `m_frameTimer->stop()` + `m_latest.clear()`（[:76-79](lab1/display_viewmodel.cpp#L76-L79)）。
- 「把模式切回空闲」→ `setMode(Mode::Idle)`（[:72](lab1/display_viewmodel.cpp#L72)）。

**关键考点：跨线程显示怎么做的。** 采集线程只往 `m_latest`（`LatestImageSlot`，单槽"最新帧"缓冲）写，**绝不碰 `QWidget/QPixmap`**；GUI 线程的 33ms `QTimer` 调 `pullFrame()`，用 `takeLatest()` 取走并 `emit frameChanged`。两端都是 ~30Hz，旧帧被静默丢弃。这是全工程的统一模式。

### 2.2 lab2 管线与 Sobel

**`capture_source.cpp`** — `next()` 三空

- 「已请求停止或源取不到帧时返回空」→ `if (m_stopped.load()) return nullptr;` 与 `if (!frame || frame->mat.empty()) return nullptr;`（[:41-46](lab2/capture_source.cpp#L41-L46)）。
- 「测试图片模式下稍作等待以模拟帧率」→ `if (!m_imagePath.isEmpty()) sleep_for(33ms)`（[:48-49](lab2/capture_source.cpp#L48-L49)）。
- 「把取到的帧打包成共享帧返回」→ `make_shared<const labcore::Frame>(std::move(*frame))`（[:51](lab2/capture_source.cpp#L51)）。

**`sobel_stage.cpp`** — `process()` 两空

- 「校验输入帧有效，无效则返回空」→ `if (!input || input->mat.empty()) return nullptr;`（[:15-16](lab2/sobel_stage.cpp#L15-L16)）。
- 「按当前参数做 Sobel 边缘检测」→ `cv::Sobel(input->mat, edges, CV_16S, dx, dy, ksize)` 后 `cv::convertScaleAbs(edges, edges)`（[:24-26](lab2/sobel_stage.cpp#L24-L26)）。
  - **为什么用 `CV_16S`**：Sobel 的 dx/dy 会产生负值和超出 8 位的梯度，必须用 16 位有符号中间结果。
  - **为什么 `convertScaleAbs`**：取绝对值并饱和转换回 `CV_8U`，才能显示。
  - 参数快照用 `m_paramsMutex` 保护（`setParams` 在 GUI 线程、`process` 在工作线程），符合"算法参数热更新"的设计。

**`pipeline_viewmodel.cpp`** — `startInternal()` 两空

- 「勾选 Sobel 时，按顺序装配'灰度 + 边缘'两个 stage」+「注意顺序不能反」→
  ```cpp
  m_pipeline->addStage(m_registry.create("gray"));
  m_pipeline->addStage(m_registry.create("sobel"));
  ```
  （[:143-146](lab2/pipeline_viewmodel.cpp#L143-L146)）
- **顺序为什么不能反**：`SobelStage` 的 `inputFormat()` 声明为 `Gray8`（[sobel_stage.h:23](lab2/sobel_stage.h#L23)），管线在 `build()` 时校验格式链；反了就变成"边缘→灰度"，`SobelStage` 拿到非 `Gray8` 输入，格式链校验失败。
- 未勾选时**不装任何 stage**，直接显示原始彩色帧（[:141-146](lab2/pipeline_viewmodel.cpp#L141-L146)）。
- 另两个考点：`m_pipeline->setSource(m_source.get())` 是**借用**语义，ViewModel 保留 `m_source` 所有权，所以停止管线后摄像头可复用；`m_source` 只在目标（摄像头/图片）变化时才重建（[:123-134](lab2/pipeline_viewmodel.cpp#L123-L134)）。
- 补一条文档原先没写的机制：**`SobelStage` 没有覆写 `execution()`，取的是基类默认的 `OwnThread`**（单独开一个工作线程并插一级队列）；而 `GrayStage` 显式写了 `Inline`，在源线程内联执行。所以 `setOutput` 回调实际跑在哪个线程**取决于 Sobel 开关**：勾选时是 sobel 的线程，未勾选时就是源线程。基类默认值定义在 `common/core/stage.hpp`，此处是否为刻意选择无法从代码判断。

### 2.3 lab5 Amsler 视物变形

**`amsler_test.cpp`** — 6 空，状态机

枚举（[amsler_test.h:12-14](lab5/amsler_test.h#L12-L14)）：`LinePattern{Backslash, Slash, Cross}`、`Answer{Normal, Distorted}`、`Phase{Idle, ShowingStimulus, WaitingAnswer, Finished}`。

- `start()` 三空：「生成区域访问顺序并随机打乱」→ `std::iota` 生成 0..N-1 后 `std::shuffle(..., m_rng)`（[:33-35](lab5/amsler_test.cpp#L33-L35)）；「复位变形标记与游标计数」→ 所有 `distorted=false`，`m_cursor=m_lineIndex=m_sampled=0`（[:36-39](lab5/amsler_test.cpp#L36-L39)）；「进入显示刺激阶段」→ `m_phase = m_order.empty() ? Finished : ShowingStimulus`（[:41](lab5/amsler_test.cpp#L41)）。
- `answer()` 三空：「未在测试中就忽略」→ `if (m_phase != WaitingAnswer || m_order.empty()) return;`（[:47-48](lab5/amsler_test.cpp#L47-L48)）；「答弯曲直接标记并前进」→ `currentRoi().distorted = true; advanceRoi();`（[:49-52](lab5/amsler_test.cpp#L49-L52)）；「答正常需累计够线型数才前进」→ `++m_lineIndex; if (m_lineIndex >= samplesPerRoi) advanceRoi(); else m_phase = ShowingStimulus;`（[:55-59](lab5/amsler_test.cpp#L55-L59)）。

**核心规则（必背）**：`samplesPerRoi = 3`，即**同一区域要连续答 3 次"正常"才算通过**，而**答 1 次"弯曲"立刻判定该区域变形并跳到下一区域**。每个区域依次显示 `\`、`/`、`X` 三种线型（`patternFor()` 用 `lineIndex % 3`）。

`stimulusTimeout()` 只在 `ShowingStimulus` 时把阶段推进到 `WaitingAnswer`——刺激显示由外部定时器驱动。

### 2.4 lab6 注视点检测

**`eye_detection.cpp`** — `detectDarkEyeTarget()` 4 空

- 「校验帧与 ROI；为空或越界时填 error 并返回」→ `frame.empty()` 检查；`params.roi & frameRect` 求交保证不越界；`roi.empty()` 时报 `"eye ROI is outside the frame"`（[:12-22](lab6/eye_detection.cpp#L12-L22)）。
- 「转灰度并去噪」→ 按 1/3/4 通道分别处理（`clone()` / `COLOR_BGR2GRAY` / `COLOR_BGRA2GRAY`），再 `cv::GaussianBlur(..., Size(5,5), 0.0)`（[:24-36](lab6/eye_detection.cpp#L24-L36)）。
- 「反转二值化后找最大轮廓」→ `cv::threshold(gray, binary, clamp(threshold,0,255), 255, cv::THRESH_BINARY_INV)` + `findContours(RETR_EXTERNAL, CHAIN_APPROX_SIMPLE)` + `max_element` 按 `contourArea` 取最大（[:37-50](lab6/eye_detection.cpp#L37-L50)）。
  - **为什么 `THRESH_BINARY_INV`**：瞳孔是暗的，反转后瞳孔变成白色前景，才能用 `findContours` 找。
  - **为什么取最大轮廓**：瞳孔是 ROI 内面积最大的暗区。
- 「用轮廓包围盒中心作为瞳孔位置（ROI 局部坐标）并置 found」→ `center = bounds.tl() + Point(w/2, h/2)`，`found = true`（[:51-57](lab6/eye_detection.cpp#L51-L57)）。默认参数 `DetectParams{ roi{0,0,50,50}, threshold=55 }`。

**`measurement_worker.cpp`** — `measureOnce()` 3 空

- 「在超时内反复取帧，丢弃首帧以稳定曝光」→ `deadline = now() + timeout`，`discardedFirstFrame` 标志跳过第一帧（[:121-130](lab6/measurement_worker.cpp#L121-L130)）。
- 「对后续帧做检测，识别成功即返回结果」→ `if (detection.found) return detection;`（[:132-135](lab6/measurement_worker.cpp#L132-L135)）。
- 「超时或中途收到停止请求则返回空」→ 循环条件 `!m_stopRequested.load() && now() < deadline`，退出后 `return std::nullopt`（[:123](lab6/measurement_worker.cpp#L123)、[:137](lab6/measurement_worker.cpp#L137)）。
- **为什么丢弃首帧**：摄像头刚打开时自动曝光/增益未收敛，首帧偏暗，二值化结果不可靠。
- 另两个函数值得知道：`judgeBinocularOffset()`（[:61-77](lab6/eye_detection.cpp#L61-L77)）用左右眼相对标定位置的**水平偏移**判定 `Centered/Left/Right/Divergent/DetectionFailed`（默认容差 4px）；预览线程 `runPreview()` 在 `m_stopped` 标志下循环，并用 `mirrorHorizontal()` 做水平镜像。
- **但 `judgeBinocularOffset()` 全仓库没有任何调用点（grep 可验证），`Status::Divergent` 也无处产生**（只有 `verdictText` 里有对应文案）。lab6 实际跑到的测量路径是 `CalibrationSession::evaluateAgainst()` 的**单眼**判定。所以上面那五种判定是「函数支持」而不是「实际行为」，被问到双眼偏移判定时要照实说明。
- **另一处坐标系不一致**：预览路径先 `mirrorHorizontal()` 再检测，而 `measureOnce()` 不做镜像，两者的 ROI 与锁定中心却是共用的。注释里只记了事实，其对视物判定结论的影响未做定论。

---

## 3. 新建的四个 lab（3 / 4 / 7 / 8）

`0af1041` 初始化时**只有 lab1、lab2、lab5、lab6**。lab3、lab4、lab7、lab8 是后加的。注意 readme 的"目录结构"一节并没有列出 lab3/4/7/8。

### 3.1 lab3 低视力成像模拟

窗口标题「低视力成像模拟」。摄像头或图片 → 四种病理视觉效果 → 实时显示。

| 效果 | 算法 | 关键参数 |
|---|---|---|
| 视力模糊 `blur_two` | `cv::GaussianBlur` | `kernel = 1 + 2*((sev+4)/5)`（恒为奇数，3→41）；`sigma = 0.4 + sev*0.08`（0.48→8.4） |
| 视野缺失 `visual_field_loss` | 自建高斯暗斑掩膜 + `cv::min` | `mask = 1 - strength*exp(-r²/(2σ²))`，中心乘 `1-strength`；`σ = max(12, 0.48*min(W,H)*(1-0.65*strength))` |
| 白内障 `cataract` | `cv::addWeighted` 混暖色 + `convertTo` 降增益 | 暖色 `Scalar(38,183,255)`（琥珀色）；增益 `1-0.35*amount` |
| 眩光 `glare` | 降采样→阈值→`GaussianBlur`→`multiply`→上采样→`add` 加性泛光 | 阈值 `165 - sev/3`（165→132）；泛光 `σ = 3.0+sev*0.08`；增益上限 0.8 |

四个函数签名统一为 `cv::Mat apply(const cv::Mat&, int severity)`，且 **`severity<=0` 时直接 `source.clone()` 返回**——这就是"复选框取消勾选"的实现方式，不是条件分支跳过。

处理链固定顺序：`blur → fieldLoss → cataract → glare`，每级在 severity=0 时为空操作。

Qt 侧：4 组「复选框 + 0–100 滑条」（`setRange(0,100)`、默认 0），一个 `QLabel` 显示画面，状态栏。连接逻辑是：复选框 `toggled` → `setXxx(on ? slider->value() : 0)`；滑条 `valueChanged` → `if (checkbox->isChecked()) setXxx(value)`。**所以取消勾选会把参数强制归零**，勾回来才恢复滑条值。

线程：ViewModel 自己起 `std::thread`（`workerLoop`），33ms `sleep`；GUI 线程 33ms `QTimer` 取 `m_latest` 发 `frameChanged`；参数用 `m_paramMutex` 保护并**每帧整份拷贝**。

### 3.2 lab4 综合视觉模拟

窗口标题「综合视觉模拟程序」。**lab4 没有自己的算法和 ViewModel**，它通过 `lab4.pro` 直接编译 lab3 的 5 个源文件（`../lab3/low_vision_viewmodel.cpp` + 四个效果模块），`INCLUDEPATH` 加 `../lab3`。所以算法行为与 lab3 完全一致。差别只在 UI：

1. 摄像头按钮改成**开关**：`if (m_viewModel->running()) stop(); else startCamera();`
2. **新增「Sobel边缘检测」复选框**（`connect(sobelCheck, &QCheckBox::toggled, m_viewModel, &LowVisionViewModel::setSobel)`）——这一条很关键：`setSobel` 在 lab3 里也存在，但 **lab3 的界面上没有任何控件调用它**，只有 lab4 真正打开了这个分支。
3. 效果行改为循环 + `QStringList` + 滑条指针数组生成，而不是 lab3 的手写四份。
4. 画面标签 `setMinimumSize(720,450)`（lab3 是 640×420），窗口 `resize(980,800)`（lab3 是 900×760）。
5. `mainwindow.h` **没有 `Q_OBJECT` 宏**（因为不声明自己的信号槽，只用 lambda + `tr()`），因此不生成 `moc_mainwindow.cpp`。lab3 有。
6. 所有权修正：lab4 用裸指针 `new LowVisionViewModel(this)`；lab3 用 `std::make_unique` **同时**又把 `this` 传作 QObject 父对象，理论上析构时会双重释放。但这一点**未实测**（Qt 的 `QObject` 析构会把自己从父对象的 children 列表里摘除，所以也可能不会二次 `delete`），本机编译不了 Qt，答辩时别当已证实的结论。

Sobel 分支在 ViewModel 的 `process()` 末尾：`cvtColor(BGR2GRAY)` + `Sobel(CV_16S, 1, 0, 3)` + `convertScaleAbs`，由 `std::atomic_bool m_sobelEnabled`（默认 false）控制。

### 3.3 lab7 YuNet 人脸与眼睛 ROI 检测（TensorRT）

窗口标题「lab7 YuNet 人脸与眼睛 ROI 检测（TensorRT）」。摄像头 → TensorRT 推理 → 画框。**没有测量、没有录制、没有文件 IO**。

流程：`开始检测` → `DetectorWorker::start()`（主线程）→ 解析模型路径 → `YuNetTRT::init(onnx, engine, /*useFp16*/true, /*forceRebuild*/false)` → `FrameSource::open()` → 起 `std::thread` 跑 `loop()` → 每轮 `next()` → `detect(mat, 0.6f, 0.3f, 5000)` → `annotate()` → 存 `QImage`（加锁）→ 主线程 33ms `QTimer` 取出 → `frameChanged` → `setPixmap`。

模型路径 `findPath()`（[detector_worker.cpp:16-26](lab7/detector_worker.cpp#L16-L26)）依次试：cwd、`applicationDirPath()/../`、`applicationDirPath()/../../`。**这三个候选就是空被填掉的那次改动之一**（原本用 `std::filesystem::is_regular_file`，后改为 `QFileInfo::isFile()`）。按 build 到 `build/` 的方式，命中的是第三个 `lab7/build/../../yunet_trt_cpp/models/...`。

绘制（`annotate()`）：人脸框绿色 2px；5 个关键点红色实心圆 r=3；`computeEyeRois(face, 0.6f)` 得到双眼 ROI，**索引 0 = 右眼画蓝框「R」，索引 1 = 左眼画红框「L」**，均 2px。

**`computeEyeRois` 的语义（必背）**：边长 = `0.6 × 双眼关键点欧氏距离`（即 3/5 瞳距）的正方形，以该眼关键点为中心。YuNet 本身**不输出眼睛框**，只输出人脸框 + 5 个关键点（顺序：右眼、左眼、鼻尖、右嘴角、左嘴角），眼框是靠关键点推算的。

**lab7 的注意点**：这里算出的眼框**纯粹是画给人看的**，lab7 里没有任何代码消费 `eyeRois`——没有瞳孔搜索、没有测量、没有门控。

线程：`DetectorWorker` 是 QObject 但**亲和主线程**，所以它的 `QTimer` 也在主线程；真正跑在工作线程的只有 `loop()`。工作线程在**正常帧通路上**不 emit 任何信号，只往 `m_latest` 写；主线程定时器取出后再 emit。**但错误路径会 emit**——`loop()` 里两处 `errorOccurred` 就是从工作线程发出的，靠 Qt 队列连接投递回 GUI 线程。所以「工作线程从不 emit」这句话只对帧通路成立，被追问时别说过头。节奏上生产者 20ms（≲50Hz）、UI 33ms（~30Hz），故意解耦、自动丢帧。

参数全是编译期字面量，**界面上没有任何滑条/输入框**：`score=0.6f`、`nms=0.3f`、`topK=5000`、眼框比例 `0.6f`。

### 3.4 lab8 综合注视门控与视物变形检测

窗口标题「lab8 综合注视门控与视物变形检测」。= **用 lab7 的 YuNet 检测做"注视监视器"，去门控 lab5 的 Amsler 视物变形测试**。

左栏（伸缩 3）：`AmslerWidget` + 三个按钮「开始 Amsler 测试」「正常」「弯曲」。
右栏（伸缩 2）：「启动 lab7 注视检测」「停止注视检测」+ 摄像头画面 + 状态栏。

**注视判定链**：摄像头 → `detect(mat, 0.6f, 0.3f, /*topK*/20)`（注意 topK 从 5000 降到 20）→ 取 score 最大的脸 → `computeEyeRois(face, 0.6f)` → 每个眼 ROI 内找瞳孔 `findPupil()`（`clippedRect` 先与画面求交，这点比 lab7 严谨）→ 两眼瞳孔都找到时取**中点** → 与**自标定基线**比较 → `cv::norm(current - centre) < 22.0` 判为注视有效。

`findPupil` 与 lab6 同构：`GaussianBlur(5,5)` → `threshold(55, THRESH_BINARY_INV)` → `findContours(RETR_EXTERNAL)` → 取最大轮廓 → 包围盒中心；画黄色十字（`drawMarker`, `MARKER_CROSS`, 10, 2）。

**自标定**：`m_haveCentre` 为假时，第一帧"两眼瞳孔都检测到"的画面就成为基线（那一刻距离为 0，必然判有效）。**没有重新标定路径**，只能靠 `stop()`/`start()` 重置。容差 22px 是硬编码。

**`fixationChanged` 是边沿触发**：只有 `valid != m_lastValid` 时才 emit。`stop()` 时若原本为 true，会补发一次 `false`。

**Amsler 门控状态机**（注意：**lab8 的 `AmslerTest` 与 lab5 的不是同一份**，是两套独立实现）：

- lab8 的枚举多一个阶段：`Phase{Idle, ShowingStimulus, WaitingAnswer, Finished, WaitingFixation}`；`GridConfig` 只有 `{rows=8, cols=8, samplesPerRoi=3}`（lab5 那份还有 `logicalWidth/logicalHeight`）。
- `setFixationValid(valid)`：**失去注视** → 若正在测试则 `invalidateCurrentTrial()` 并转 `WaitingFixation`；**恢复注视** → 从 `WaitingFixation` 回到 `ShowingStimulus`，**继续同一个 ROI**。
- `invalidateCurrentTrial()` 只重置 `m_lineIndex=0` 和阶段，**不动 `m_cursor`、`m_sampled`、任何 `distorted` 标记**。即：丢失注视既不跳过区域、也不算作答，只是让当前区域那 3 次线型重头再来。
- 判定规则同 lab5：**连答 3 次「正常」清掉一个区域，答 1 次「弯曲」立刻标记该区域变形并前进**。每区域依次显示 `\`、`/`、`X`（`patternFor` 用 `% 3`）。
- 计分：`sampledRois = m_sampled`，`distortedRois` = 被标记的区域数，**每个区域最多贡献 1**。满跑 8×8 报告 `sampledRois = 64`，最多 64×3=192 次刺激。最终文案「测试完成：%1 个区域中 %2 个出现变形」。

**绘制**（`AmslerWidget::paintEvent`）：白底。`Idle` 或 `finished` 时画完整网格 `drawGrid()`（黑 1px 方框，每格左上角写**1 起始**的行优先编号）；测试中则**完全不画网格**，只画当前格（黑 4px 框 + 对角线，`Cross` 画两条交叉成 X）——刻意不给受试者方位线索。然后**始终**在控件中心画一个 **10px 宽的红点**作为注视目标。注视无效时深红字提示「注视无效，测试暂停」。

**线程**：与 lab7 不同——`FixationMonitor` **没有 QTimer**，工作线程**直接 `emit frameChanged(...)`**，靠 Qt 队列连接把 `QImage`（已由 `toQImage` 深拷贝）投递到 GUI 线程。状态（`m_haveCentre/m_centre/m_lastValid`）由 `m_stateMutex` 保护。Amsler 状态机与绘制全在 GUI 线程，**零同步**，因为 `fixationChanged` 是队列投递的槽调用。节拍 33ms。

参数同样无 UI：`samplesPerRoi=3`、闪烁 250ms 单次触发、注视容差 22px、瞳孔阈值 55、眼框比例 0.6f、topK 20。

---

## 4. 每个实验的流程（通俗版）

> 这一节是大白话版，答辩时可以直接照着讲。前面几节是精确版，两边说法不一致时以前面为准。

### lab1 图像与视频显示

- **干什么**：在窗口里显示一张图片，或者一路摄像头画面，二选一。
- **操作**：点「加载图片」选一张图 → 显示这张图。点「开启摄像头」→ 变成实时画面，并且左右是镜像的（像照镜子，符合前置摄像头的习惯）。再点同一个按钮就关掉。开摄像头会自动顶掉图片，两者互斥。
- **内部怎么走**：摄像头在后台线程里不停取帧；界面每 33 毫秒（约每秒 30 次）去取一次「最新的那一帧」来画，中间没被取走的旧帧直接丢掉。
- **为什么非要绕这一圈**：界面控件只能在主线程里碰，后台线程直接画会崩。所以后台只管往一个「最新帧」格子里写，主线程只管从格子里取。
- **一句话**：后台只管取，主线程只管画，中间放个格子。

### lab2 管线与 Sobel 边缘检测

- **干什么**：给摄像头画面做边缘检测（Sobel 算子），能随时开关。
- **操作**：开摄像头 → 先是彩色原图 → 勾上「Sobel」→ 变成黑白轮廓线 → 取消勾选又变回彩色。状态栏会显示每秒实际处理多少帧、扔掉了多少帧。
- **内部怎么走**：把处理拆成「源 → 灰度 → 边缘」三段，串成一条流水线，每一段的输出就是下一段的输入。中间那步灰度是**必需**的：边缘检测只吃单通道图，管线会检查这个，顺序接反了直接失败。
- **一句话**：像水管一样一节一节接起来，每节只干一件事。

### lab3 低视力成像模拟

- **干什么**：把正常画面变成眼病患者看到的样子，四种效果可以叠加。
- **操作**：开摄像头或加载图片 → 勾上某个效果前面的框 → 拖它后面的滑条调强度（0–100）→ 画面实时变化。
- **四个效果的代码实现**：每个效果一个独立文件夹（`lab3/blur_two/`、`visual_field_loss/`、`cataract/`、`glare/`），每个文件夹里就一个 `apply(图像, 强度)` 函数，函数没有状态、逐帧调用，四个串成固定顺序 `blur → fieldLoss → cataract → glare`。

  1. **视力模糊 `blur_two`**——整幅高斯模糊，就一句 `cv::GaussianBlur`，但两个参数都由强度算出来：
     - 核边长 `1 + 2*((强度+4)/5)`。先 `+4` 是为了让整数除法变成向上取整，再 `×2+1` 保证结果**恒为奇数**（OpenCV 要求核必须是奇数）。强度 1 对应 3×3，强度 100 对应 41×41。
     - 模糊半径 `sigma = 0.4 + 强度*0.08`（0.48 → 8.4）。为什么要额外给 sigma：核边长是一档一档跳的，sigma 连续变化，画面糊的程度才会跟着滑条**平滑**变，不会一顿一顿。
  2. **视野缺失 `visual_field_loss`**——画面中心一块发暗、越靠外越亮。做法是**自己逐像素生成一张「暗斑掩膜」，再和原图逐通道取 min**：
     - 掩膜公式 `1 - 强度 * exp(-r²/(2σ²))`，`r` 是像素到画面中心的距离。中心处 `exp=1`，掩膜最暗；远离中心 `exp→0`，掩膜→1，等于不动原图。
     - 暗斑大小 `sigma = max(12, 0.48 × 画面短边 × (1 - 0.65×强度))`。注意**强度越大 sigma 越小**——暗斑更集中、中心更黑、剩下能用的视野更小，这才像病情加重。下限 12 是防止小图或重症时 sigma 趋近 0。
     - 为什么不用 `GaussianBlur` 而手写双重循环：这里要的是「以中心为峰值的衰减掩膜」，不是归一化的卷积核，两回事。
     - 为什么用 `min` 而不是乘/减：取 min 是**只压暗、不增亮**，掩膜为 1 的地方像素原样保留。中途转成 32 位浮点运算，避免 8 位整数的舍入误差。
  3. **白内障 `cataract`**——整体蒙一层发黄的雾，同时变暗。两步：
     - 混色：`cv::addWeighted` 把原图和一层纯琥珀色按强度混合。琥珀色按 BGR 写成 `(38, 183, 255)`（蓝最低、红最高，就是黄褐色）。
     - 降增益：`convertTo` 乘 `(1 - 0.35×强度)`，最重时整体压暗 35%。
     - 为什么要两步：只混色只是「变黄」，再压暗才像晶状体混浊——混浊是既偏黄又透光变差的。
  4. **眩光 `glare`**——亮的地方往外晕开光晕，六步走：
     - 先缩到 **1/4 分辨率**再处理：同样大的模糊核在缩略图上等效覆盖 4 倍范围，计算量却只剩约 1/16，最后再放大回去。
     - 找亮区：转灰度后按阈值二值化，阈值 `165 - 强度/3`。**强度越大阈值越低**，越多地方被判成亮区，泛光范围就越大。
     - 糊成泛光：`GaussianBlur`，`sigma = 3.0 + 强度*0.08`。
     - 泛光的颜色取自**原图**而不是纯白，所以亮处晕开的是带该处色彩的光，不会糊成一片死白。
     - 最后用 `cv::add` **加性**叠回原图（不是覆盖）；增益封顶 0.8，防止整个画面过曝。

- **一个关键设计**：取消勾选**不是**「跳过这个效果」，而是把它的强度设成 0——四个 `apply()` 的第一行都是 `if (severity <= 0) return source.clone();`，参数归零就是空操作，所以调用处不必写「这个效果开没开」的分支。
- **一句话**：四个滤镜串起来，每个都能单独调强度。

### lab4 综合视觉模拟

- **干什么**：和 lab3 是同一套效果，界面重新做了一版。
- **操作**：和 lab3 一样，但有两处不同——摄像头按钮变成了开关；另外多了一个「Sobel 边缘检测」复选框。
- **内部怎么走**：lab4 自己没有算法代码，它的工程文件直接编译 lab3 的源文件，所以两种效果一模一样。那个 Sobel 开关在 lab3 的代码里也存在，但 **lab3 的界面上没有任何控件能触发它**，只有 lab4 真正打开了这个分支。
- **一句话**：lab3 的算法 + 一版新界面 + 多一个边缘检测开关。

### lab5 Amsler 视物变形测试

- **干什么**：眼科查黄斑变性的经典表格测试，做成电子版。
- **操作**：点「开始测试」→ 屏幕正中有个红点，受试者全程盯着它 → 某一格闪出一个图案 → 受试者判断看着弯不弯，点「正常」或「弯曲」→ 换下一格 → 全部做完后报告有几个格出现变形。
- **规则**：格子是 8×8。每个格子依次显示三种图案（`\`、`/`、`X`）。同一个格子要**连着答 3 次「正常」**才算通过；只要答 1 次「弯曲」，立刻判定这格变形，跳到下一格。
- **一个关键设计**：测试进行中**不画完整网格**，只显示当前那一格——否则受试者能靠网格的位置猜出自己看到的是哪一格，结果就不可信了。只有没开始或已结束时才画完整网格（带编号）。
- **一句话**：红点盯着，格子一个个闪，有变形就记一笔。

### lab6 注视点检测与标定

- **干什么**：用摄像头找眼睛瞳孔的位置，判断眼睛有没有看偏。
- **操作**：开摄像头 → 把眼睛框进取景框（ROI）→ 点「开始校准」，让系统记住眼睛此刻在画面里的位置 → 停止预览 → 点「测量」，系统在 2 秒内连拍多帧找瞳孔，和刚才记住的位置比偏移。
- **怎么找瞳孔**：把取景框那块抠出来 → 转灰度 → 模糊去噪 → 二值化并反转（瞳孔是暗的，反转后变成白块）→ 在所有白块里挑最大的那个 → 它的中心就是瞳孔。
- **为什么要丢掉第一帧**：摄像头刚打开时自动曝光/增益还没收敛，第一帧整体偏暗，二值化结果不准。
- ⚠️ **实测提醒**：现在点「测量」会直接显示「采集超时」，出不来结果（原因：点测量会先停掉预览并置位 `m_stopRequested`，`measureOnce()` 的循环一次都不执行）。预览阶段能正常看到瞳孔十字。答辩时按实际现象说。
- **一句话**：先记住眼睛在哪，再拿新画面跟它比。

### lab7 YuNet 人脸与眼睛 ROI 检测（TensorRT）

- **干什么**：用 GPU 加速的人脸检测模型，在画面里框出人脸和五官。
- **操作**：点「开始检测」→ 画面出现绿框（人脸）、红点（5 个关键点：两眼、鼻尖、两个嘴角）、蓝框标「R」（右眼）和红框标「L」（左眼）。
- **眼框是怎么来的**：模型本身**不输出眼睛框**，只给人脸框和 5 个关键点。那两个眼框是算出来的——以眼睛关键点为中心，边长取「两只眼睛关键点距离」的 0.6 倍。这里算出来**纯粹是画给人看的**，没有别的地方用它。
- **模型怎么找**：程序按三个位置依次找模型文件（当前目录、程序目录上一级、上上级），哪个先找到用哪个。第一次运行会拿 ONNX 模型在 GPU 上现场编译出一个 engine 文件存下来，以后直接加载它省时间。注意 engine **换显卡或换 TensorRT 版本就不能用了**，得用 ONNX 重建。
- **一句话**：模型给脸和关键点，眼框自己按比例算。

### lab8 综合注视门控与视物变形检测

- **干什么**：lab7 的注视检测 + lab5 的 Amsler 测试，合成一个——**只在受试者盯着看的时候才测**。
- **操作**：先点「启动注视检测」→ 画面出现红点（注视目标）→ 点「开始 Amsler 测试」→ 和 lab5 一样作答。如果受试者视线移开太多，测试**自动暂停**并提示「注视无效，测试暂停」，盯回来之后从当前格子重新开始。
- **为什么要门控**：Amsler 测试要求受试者全程盯着中心红点，眼睛跑了结果就不作数。所以拿 lab7 的检测当「监视器」用。
- **怎么判断在不在看**：找到两只眼睛的瞳孔，取它们的中点，和一个基准位置比，距离小于 22 像素就算在看。基准位置是启动检测后**第一帧能同时找到两个瞳孔**的画面（自动标定一次；之后没有重新标定的入口，只能停掉重开）。
- ⚠️ **操作注意**：图案闪烁的 250 毫秒内按键会被忽略，但计时会重新开始。连点得比 250 毫秒还快的话，测试会一直卡在原地不前进。等闪完再按。
- **一句话**：眼睛没盯着就暂停，盯回来了接着测。
